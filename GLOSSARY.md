# 용어집

OmniLog 코드와 문서에 나오는 용어 정리. 각 항목은 **뜻**과 **이 프로젝트에서 실제로
어디에 쓰였는지**를 함께 적었습니다.

- [위키 도메인](#위키-도메인)
- [저장과 데이터베이스](#저장과-데이터베이스)
- [검색](#검색)
- [마크업과 렌더링](#마크업과-렌더링)
- [웹과 API](#웹과-api)
- [문자와 인코딩](#문자와-인코딩)
- [도구](#도구)

---

## 위키 도메인

### slug · 슬러그

문서를 URL에서 가리키는 짧은 식별자. 제목을 소문자로 바꾸고 공백을 하이픈으로
치환한 것.

**어원** — 신문사 은어입니다. 활판 인쇄에서 한 줄을 통째로 주조한 금속 조각을 slug라
불렀고, 거기서 편집국에서 **기사에 붙이는 짧은 식별용 이름**을 뜻하게 됐습니다. 화재
기사면 `FIRE` 같은 식으로, 제목이 확정되기 전에도 조판 시스템에서 그 기사를 가리키려고
붙이는 꼬리표입니다. Django가 캔자스의 Lawrence Journal-World 신문사에서 만들어지면서
`SlugField`라는 이름으로 코드에 들어왔고, 거기서 웹 개발 전반에 퍼졌습니다. 번역어가
없어서 한국에서도 그냥 "슬러그"라고 씁니다.

**용도 1 — URL에 안전하게 넣기.** 제목을 그대로 주소에 쓰면 공백과 슬래시가 경로를
깨뜨립니다.

```
제목    김치 찌개 / 레시피!!
슬러그  김치-찌개-레시피
```

**용도 2 — 표기가 흔들려도 같은 문서로 모으기.** 실질적으로는 이쪽이 더 중요합니다.
슬러그는 **매칭 키**입니다.

```
'김치 찌개'  '김치  찌개'  '김치/찌개'  '  김치 찌개  '  →  '김치-찌개'
```

이게 없으면 "누가 `[[김치 찌개]]`라고 건 링크가 제목 `김치찌개`인 문서와 같은
것인가"를 매번 판단해야 합니다. 슬러그가 있으면 양쪽 다 같은 값으로 떨어져 자동으로
만납니다.

**변환 규칙** — `omnilog/slug.py`, 8단계입니다.

```
NFC 정규화 → 앞뒤 공백 제거 → 소문자화 → 공백·슬래시를 하이픈으로
→ 영숫자와 -_. 외 전부 하이픈 → 연속 하이픈 축약 → 앞뒤 -. 제거 → 200자 자르기
```

[NFC 정규화](#nfc--nfd--유니코드-정규화)가 한글에서 특히 중요합니다. macOS가 보내는
자모 분리형(NFD) `한국어`는 9바이트가 아니라 24바이트고 파이썬 비교에서 `==`가
`False`입니다. 정규화를 빠뜨리면 겉보기에 똑같은 문서가 두 개 생깁니다.

남는 글자가 없는 제목(`!!!`, `@#$%`)은 `400 invalid_slug`입니다.

**이 프로젝트에서** — 한글은 하이픈으로 뭉개지 않고 그대로 둡니다. 많은 슬러그 구현이
비ASCII를 통째로 버려서 `한국어 제목`이 빈 문자열이 되는데, 그러면 한국어 위키에서 쓸
수가 없습니다.

슬러그가 등장하는 곳은 넷입니다:

| 위치 | 역할 |
|---|---|
| URL | `/api/pages/김치-찌개` |
| `page.slug` | UNIQUE — 위키 전체에서 유일 |
| `pagelink.to_slug` | 링크가 가리키는 이름 (아직 없는 문서일 수도) |
| `redirect.from_slug` | 이름을 바꾼 뒤 남는 옛 이름 |

**슬러그는 주소이지 정체성이 아닙니다.** 문서의 정체성은 절대 바뀌지 않는 `page.id`
쪽이고, 슬러그는 언제든 바뀔 수 있는 라벨입니다. 이름 변경이 값싼 이유가 여기
있습니다.

| | 값 | 바뀌나 |
|---|---|---|
| `page.id` | `1` | 절대 안 바뀜 — 정체성 |
| `page.slug` | `김치-찌개` | 언제든 — 주소 |
| `page.title` | `김치 찌개` | 언제든 — 표시 이름 |

### page · 문서

위키의 한 항목. 슬러그·제목·현재 리비전 포인터를 갖습니다.

**이 프로젝트에서** — `page` 테이블. 본문은 여기 없습니다. 파일이 아니라 DB 행이므로
"문서가 어디에 있다"는 위치 개념 자체가 없습니다. rename은 컬럼 값 하나를 바꾸는
일입니다.

### revision · 리비전, 개정

한 번의 저장으로 만들어지는 문서의 한 판본. 편집할 때마다 새로 쌓이고 기존 것은
수정되지 않습니다.

**이 프로젝트에서** — `revision` 테이블. 문서별로 1부터 번호가 붙고 `parent_id`로
직전 리비전을 가리켜 사슬을 이룹니다.

### diff

두 판본의 차이. **unified diff**는 `-`/`+` 줄과 `@@` 위치 표시를 쓰는 표준 형식.

**이 프로젝트에서** — `GET /api/pages/{slug}/diff`. 표준 라이브러리
`difflib.unified_diff`로 생성합니다.

### revert · 되돌리기

과거 리비전의 내용을 되살리는 것.

**이 프로젝트에서** — 이력을 고쳐 쓰지 않고 과거 내용을 새 리비전으로 다시
저장합니다. 3번 리비전에서 1번으로 되돌리면 4번이 생깁니다.

### null edit · 널 편집

제목과 본문이 현재와 완전히 같은 저장. MediaWiki 용어.

**이 프로젝트에서** — 이력을 늘리지 않고 무시하며, 응답의 `changed`가 `false`로
옵니다.

### edit conflict · 편집 충돌

두 사람이 같은 판본을 기준으로 편집해서 나중 저장이 앞선 저장을 덮어쓰는 상황.
**lost update**라고도 합니다.

**이 프로젝트에서** — `PUT`에 `base_revision`을 실으면 그 사이에 판본이 바뀐 경우
`409`를 돌려줍니다.

### wikilink · 위키링크

본문 안에서 다른 문서를 가리키는 `[[대상]]` 문법. `[[대상|표시할 텍스트]]`로 라벨을
따로 줄 수 있습니다.

**이 프로젝트에서** — `omnilog/markup.py`의 인라인 규칙으로 파싱합니다.

### red link · 빨간 링크

아직 존재하지 않는 문서를 가리키는 링크. 위키에서 "여기 문서가 필요하다"는 신호로
쓰입니다. 반대는 blue link.

**이 프로젝트에서** — `class="wikilink wikilink-new"`, `data-exists="false"`. 대상
문서가 생기는 순간 파란 링크로 바뀝니다.

### backlink · 역링크

이 문서를 가리키고 있는 다른 문서들. 링크의 반대 방향 조회.

**이 프로젝트에서** — `pagelink` 테이블을 `to_slug`로 조회. 문서의 옛 이름으로
작성된 링크까지 찾습니다.

### redirect · 리다이렉트 / alias · 별칭

다른 이름으로 같은 문서에 도달하게 해주는 연결. 이름을 바꿔도 기존 링크와 URL이
깨지지 않게 합니다.

**이 프로젝트에서** — `redirect` 테이블. 슬러그가 아니라 **page id를 저장**하므로
연쇄 rename을 해도 체인이나 순환이 생기지 않고 항상 한 홉에 해결됩니다.

### namespace · 이름공간

`Talk:`, `틀:` 처럼 문서를 분류하는 접두어 체계.

**이 프로젝트에서** — 만들지 않았습니다.

### transclusion · 틀 삽입

한 문서의 내용을 다른 문서 안에 끼워 넣어 렌더링하는 기능. MediaWiki의 `{{틀}}`.

**이 프로젝트에서** — 만들지 않았습니다.

### link fixup · 링크 수리

문서 이름이 바뀌었을 때 다른 문서 본문 안의 링크 원문까지 새 이름으로 고쳐 쓰는 작업.

**이 프로젝트에서** — 하지 않습니다. 별칭이 받아주므로 동작에는 문제가 없습니다.

---

## 저장과 데이터베이스

### BLOB · Binary Large Object

DB에 그대로 담는 **바이트 덩어리**. `TEXT`가 인코딩이 있는 문자열인 것과 달리, DB가
내용을 해석하지 않고 바이트 그대로 보관만 합니다.

gzip 결과물이 대표적입니다. 압축된 바이트에는 `0x00`도 들어가고 UTF-8로 해석되지 않는
조합도 있어서 `TEXT`에 넣으면 깨집니다.

```
글자로   돼지고기와 김치를 넣고 끓인다.       ← TEXT
바이트로 1f 8b 08 00 00 00 00 00 00 0a …    ← BLOB
```

**이 프로젝트에서** — `text.data`와 `blob.data` 컬럼.

**주의: 같은 단어가 두 뜻으로 쓰입니다.** 위가 컬럼 타입으로서의 BLOB이고,
[history blob](#history-blob--묶음-블롭)은 "여러 리비전을 담은 묶음"을 뜻합니다.
Git에서는 또 달라서, blob이 파일 하나의 내용 한 판본을 가리킵니다 — 묶는 단위는
packfile입니다.

### history blob · 묶음 블롭

여러 리비전의 내용을 **한 덩어리에 담아 한 번에 압축한 것**. 압축 단위를 키우기 위한
장치입니다.

**이 프로젝트에서** — `blob` 테이블. 키프레임 구간 하나(최대 16개)의 payload를 이어붙여
gzip 한 번으로 압축합니다. 묶인 `text` 행은 `data`가 비고 블롭 안의
`blob_offset`/`blob_length`를 가리키는 포인터가 됩니다. 실측에서 본문 저장량을 추가로
54% 줄였습니다.

### compression window · 압축 창

gzip이 "아까 본 것"을 기억해두는 버퍼. 압축이란 반복을 **"N바이트 앞으로 가서 M바이트
복사"** 라는 지시로 바꾸는 것이고, 그 참조 범위가 창입니다.

**핵심은 창이 호출마다 초기화된다는 점입니다.** 같은 데이터라도 잘게 나눠 압축하면
조각마다 백지에서 시작해서 훨씬 커집니다.

```
같은 데이터 408바이트를 몇 조각으로 나누느냐:
  1조각 →  80바이트
  8조각 → 592바이트   ← 내용은 동일한데 7배
```

**이 프로젝트에서** — `text` 행 하나가 gzip 호출 하나이고 곧 창 하나였습니다. 블롭으로
묶어서 gzip 스트림을 61개에서 5개로 줄인 것이 [묶기](#history-blob--묶음-블롭)입니다.

### gzip 고정 오버헤드

gzip 결과물에는 헤더 10바이트와 푸터 8바이트가 무조건 붙습니다. 빈 데이터를 압축해도
20바이트가 나옵니다.

**이 프로젝트에서** — 48바이트짜리 델타를 압축하면 69바이트가 됩니다. **압축해서 더
커집니다.** 작은 조각을 개별 압축하는 게 왜 최악인지의 절반이 이것이고, 나머지 절반이
창 초기화입니다.

### immutable · 불변 / append-only · 덧붙이기 전용

한 번 쓰면 절대 수정하지 않는 것. append-only는 새 행을 추가할 뿐 기존 행을 고치지
않는 저장 방식.

**이 프로젝트에서** — `text` 테이블이 **논리적으로** 불변입니다. 어떤 `text.id`가
돌려주는 문자열은 절대 바뀌지 않습니다. 물리적 인코딩은 불변이 아니어서, 압축 작업이
같은 내용을 델타로 다시 인코딩할 수 있습니다. 이 구분이 중요합니다 — 렌더 캐시가
`rev_id` 하나로 키잉할 수 있는 근거는 논리적 불변성 쪽입니다.

### delta · 델타

전체 내용 대신 "직전 판본과 무엇이 다른가"만 저장하는 방식. 위키 편집은 긴 본문에서
몇 줄만 바꾸므로, 델타는 본문보다 훨씬 작습니다. gzip을 얹은 뒤에도 그렇습니다.

**이 프로젝트에서** — `omnilog/delta.py`의 줄 단위 코덱. 쓸 때는 쓰지 않고, 압축
작업에서만 적용합니다. 실측에서 `text` 테이블을 76.5% 줄였습니다.

### keyframe · 키프레임

델타 사슬을 끊기 위해 주기적으로 남겨두는 완전한 판본. 영상 코덱에서 온 말입니다.

**이 프로젝트에서** — 16개마다 하나, 그리고 각 문서의 현재 본문은 항상 전체로
남깁니다. 델타는 키프레임만 가리키고 다른 델타는 가리키지 않으므로, **어떤 리비전을
읽어도 델타 적용은 최대 한 번**입니다. Git이 packfile에서 체인 깊이를 50까지 허용하며
관리하는 것과 달리, 여기서는 체인이 아예 생기지 않습니다.

### compaction · 압축 작업 / repack

이미 저장된 데이터를 더 작은 표현으로 다시 쓰는 별도 작업. 쓰기 경로를 단순하게 두고
비용을 나중으로 미루는 방식입니다.

**이 프로젝트에서** — `POST /api/maintenance/compact`. 서로 독립적인 두 축을 돌립니다:
[델타](#delta--델타)는 리비전 사이의 중복을 없애고, [묶기](#history-blob--묶음-블롭)는
압축 단위를 키웁니다. Git의 `git gc`, MediaWiki의 리비전 재압축과 같은 자리입니다.

**Git도 커밋할 때 diff를 저장하지 않습니다** — 매번 파일 전체를 blob으로 쓰고, 델타
압축은 packfile을 만들 때만 일어납니다.

### chain · 델타 체인

각 판본을 **바로 앞 판본** 대비 델타로 저장해 사슬처럼 잇는 방식.
[키프레임](#keyframe--키프레임)과 대비됩니다.

저장은 가장 작지만, 판본 하나를 읽으려면 사슬 처음부터 델타를 전부 적용해야 합니다.
그래서 실제 시스템은 반드시 길이를 자릅니다 — Git은 `pack.depth` 기본값 50,
MediaWiki `DiffHistoryBlob`은 블롭당 100개·10MB.

**즉 순수한 체인을 쓰는 시스템은 없습니다.** 자르는 지점이 곧 키프레임이고, 차이는
간격뿐입니다.

**이 프로젝트에서** — 쓰지 않습니다. 간격 16의 키프레임이 우리 선택입니다. 체인이
감당되려면 체인 걷기가 DB 왕복이 아니라 메모리 작업이어야 하는데(MediaWiki는 블롭 하나
읽어 메모리에서 품), 우리는 리비전당 행 하나라 걷는 값이 비쌉니다.

### VACUUM

SQLite가 DB 파일을 통째로 다시 써서 빈 공간을 실제로 파일시스템에 돌려주는 명령.
행을 지우기만 하면 파일 안에 빈 페이지로 남습니다.

**이 프로젝트에서** — 압축 작업 끝에 실행합니다. **WAL 모드에서는 VACUUM 결과가
체크포인트 전까지 본 파일에 반영되지 않아서**, `PRAGMA wal_checkpoint(TRUNCATE)`까지
한 다음 크기를 보고합니다. 이걸 빠뜨리면 줄지 않은 것처럼 보입니다.

### cache eviction · 캐시 축출

캐시가 무한히 자라지 않도록 항목을 버리는 정책. 캐시에 상한이 없으면 그건 캐시가
아니라 그냥 또 하나의 테이블입니다.

**이 프로젝트에서** — 렌더 캐시는 문서당 현재 리비전 하나만 둡니다. 처음에는 조회된
모든 리비전을 캐싱했는데, 그러면 DB 파일의 57%를 차지했습니다.

### text store · 본문 저장소

본문을 메타데이터와 분리해 따로 담아두는 계층.

**이 프로젝트에서** — `omnilog/textstore.py`. 512바이트 이상이면 gzip으로 압축하고
`flags` 컬럼에 `utf-8,gzip`처럼 처리 방식을 기록합니다.

### external storage · 외부 저장소

본문 블롭을 주 DB 바깥의 별도 클러스터에 두는 MediaWiki의 구조. `text` 행에는
`DB://cluster/id` 같은 포인터만 남습니다.

**이 프로젝트에서** — 쓰지 않지만, 본문을 분리해둔 덕에 나중에 얹을 수 있는
형태입니다.

### transaction · 트랜잭션

여러 쿼리를 "전부 반영되거나 전부 취소되거나" 둘 중 하나로 묶는 단위.

**이 프로젝트에서** — `omnilog/db.py`의 `write_tx()` 컨텍스트 매니저.

### BEGIN IMMEDIATE

SQLite에서 트랜잭션을 시작하면서 쓰기 락을 즉시 잡는 방식. 기본값은 읽기로 시작했다가
쓸 때 승격(upgrade)하는데, 두 연결이 동시에 그러면 교착합니다.

**이 프로젝트에서** — 모든 쓰기가 이걸로 시작합니다.

### optimistic concurrency control · 낙관적 동시성 제어

락을 걸어 막는 대신, 편집 기준 판본을 함께 보내게 하고 저장 시점에 어긋나면 거절하는
방식.

**이 프로젝트에서** — `PUT`의 `base_revision` 필드.

### WAL · Write-Ahead Logging

SQLite의 저널 방식 중 하나. 변경분을 별도 파일에 먼저 쓰기 때문에 읽기가 쓰기를 막지
않습니다.

**이 프로젝트에서** — `PRAGMA journal_mode = WAL`. 그래서 `.db` 옆에 `-wal`, `-shm`
파일이 생깁니다.

### foreign key · 외래 키 / cascade · 캐스케이드

다른 테이블의 행을 가리키는 제약. `ON DELETE CASCADE`는 부모가 지워지면 자식도 함께
지우고, `ON DELETE SET NULL`은 가리키던 값을 `NULL`로 바꿉니다.

**이 프로젝트에서** — 문서를 지우면 리비전·링크·별칭·렌더 캐시가 캐스케이드로 함께
사라집니다. SQLite는 `PRAGMA foreign_keys = ON`을 켜야 실제로 강제합니다.

### UPSERT · ON CONFLICT ... DO UPDATE

"없으면 넣고 있으면 갱신". 조회 후 분기하는 왕복을 없앱니다.

**이 프로젝트에서** — 렌더 캐시 저장과 별칭 생성. FTS5 가상 테이블은 UPSERT를
지원하지 않아 거기서는 DELETE 후 INSERT를 씁니다.

### rowid / WITHOUT ROWID

SQLite가 모든 행에 자동으로 붙이는 정수 키. `WITHOUT ROWID`는 그걸 없애고 기본 키
자체로 저장을 조직해서, 복합 키 위주의 작은 테이블에서 공간과 조회를 아낍니다.

**이 프로젝트에서** — `pagelink`, `redirect`가 `WITHOUT ROWID`입니다.

### index · 인덱스 / inverted index · 역인덱스

인덱스는 특정 컬럼으로 빠르게 찾기 위한 보조 자료구조. 역인덱스는 반대로 "값 → 그
값을 가진 행들"로 뒤집어 놓은 것으로, 전문 검색의 기본 구조입니다.

### gzip · compresslevel

DEFLATE 기반 압축. 레벨은 1~9로 속도와 압축률을 맞바꿉니다.

**이 프로젝트에서** — 레벨 6, `mtime=0`. 타임스탬프를 0으로 고정해 같은 본문이 항상
같은 바이트가 되게 합니다.

---

## 검색

### FTS · Full-Text Search, 전문 검색

**본문 안을 뒤지는 검색.** "제목이 정확히 X인 문서"가 아니라 "본문에 X가 들어있는
문서"를 찾는 것입니다. **FTS5**는 SQLite에 내장된 전문 검색 확장입니다.

`LIKE '%위키%'`로도 되지만 그건 문서를 전부 하나씩 열어보는
[전체 스캔](#full-scan--전체-스캔)입니다. 문서 10만 개면 10만 개를 다 읽습니다.

그래서 미리 방향을 뒤집은 색인을 만들어 둡니다:

```
일반적인 방향   문서 → 그 안의 단어들
색인의 방향     단어 → 그 단어가 있는 문서들     ← 역인덱스
```

검색하면 색인에서 그 단어 항목만 찾아 거기 적힌 문서 번호를 읽습니다. 본문은 안
봅니다.

**이 프로젝트에서** — `page_fts` 가상 테이블. 색인 대상은 **각 문서의 현재 본문뿐**이며
과거 리비전은 검색되지 않습니다. 편집할 때마다 DELETE 후 INSERT로 통째로 교체합니다
(FTS5는 UPSERT를 지원하지 않습니다).

### FTS 인덱스 크기

전문 검색 인덱스는 **본문보다 큽니다.** 글자 수만큼 색인어가 생기고, 각 색인어마다
"어느 문서 어느 위치에 있는지" 목록이 붙기 때문입니다.

`page_fts`는 하나처럼 보이지만 SQLite가 뒤에 그림자 테이블 넷을 만듭니다:

| 테이블 | 담는 것 |
|---|---|
| `page_fts_data` | 색인 본체 (역인덱스 블록) |
| `page_fts_idx` | 세그먼트별 색인어 위치 |
| `page_fts_docsize` | 문서별 토큰 수 (BM25 계산용) |
| `page_fts_config` | 토크나이저 설정 |

**이 프로젝트에서** — 실측입니다. 본문 약 25KB짜리 문서 하나를 trigram으로 색인한
결과:

| | 크기 | 비중 |
|---|---:|---:|
| `page_fts_data` | 182,093 | **58%** |
| 본문 (`text`+`blob`) | 14,686 | 5% |
| `render_cache` | 2,746 | 1% |
| **DB 파일** | **315,392** | |

**본문의 약 7배**이고, 압축을 다 끝낸 지금 DB 파일에서 가장 큰 항목입니다. 한국어
부분 문자열 검색을 위해 지불한 값입니다. 줄이려면 trigram을 포기하고
[unicode61](#unicode61)로 가는 거래를 해야 합니다.

### virtual table · 가상 테이블

SQLite에서 일반 테이블처럼 보이지만 내부 동작을 확장 모듈이 담당하는 테이블. FTS5
인덱스가 이 형태입니다. 실제 데이터는 뒤에 숨은 그림자 테이블들에 들어갑니다.

### tokenizer · 토크나이저

텍스트를 검색 단위(토큰)로 쪼개는 규칙. **무엇을 "단어"로 볼지 정하는 것**이고, 검색
품질과 인덱스 크기를 둘 다 여기서 결정합니다.

본문이 `"위키시스템 설계"` 8글자일 때 실제로 만들어지는 색인어:

```
unicode61   2개   설계 / 위키시스템
trigram     6개   설계 / 스템  / 시스템 / 위키시 / 키시스 / 템 설
```

### unicode61

FTS5의 기본 토크나이저. 공백과 문장부호로 잘라 **단어 단위**로 색인합니다.

한국어에서 문제가 됩니다. `위키시스템`이 색인어 하나라서 `위키`로 검색하면 못 찾습니다.
색인에 `위키`라는 항목 자체가 없기 때문입니다. 조사가 붙고 띄어쓰기가 불규칙한
한국어에서는 자주 빗나갑니다.

대신 색인이 작습니다.

### trigram · 트라이그램

텍스트를 **세 글자씩 겹쳐가며** 색인하는 토크나이저. `위키시스템` → `위키시`, `키시스`,
`시스템`. 글자 위치마다 항목이 생기므로 **부분 문자열 검색**이 됩니다.

```
"위키시" 검색 →  unicode61 못 찾음  /  trigram 찾음
"위키"   검색 →  둘 다 못 찾음
```

**3글자 미만 쿼리는 매칭하지 못합니다.** 세 글자로 쪼갠 색인이라 두 글자로는 걸리는
항목이 없습니다.

**이 프로젝트에서** — 기본으로 씁니다. 2글자 한국어 검색어는 흔하므로 그런 쿼리는
`LIKE` 스캔으로 넘어갑니다. 인덱스가 커지는 대가는
[FTS 인덱스 크기](#fts-인덱스-크기)에 적었습니다.

### MATCH

FTS5에서 전문 검색을 수행하는 연산자. `AND`, `OR`, `NEAR`, `*` 같은 자체 문법을
갖습니다.

**이 프로젝트에서** — 사용자 입력을 인용부호로 감싸 그 문법이 문자 그대로 처리되게
합니다. 안 그러면 검색어에 섞인 `OR` 하나가 쿼리 의미를 바꾸거나 파서를 깨뜨립니다.

### BM25

문서 순위를 매기는 표준 점수 함수. 흔한 단어의 가중치를 낮추고, 컬럼별 가중치를 줄 수
있습니다. **값이 작을수록 관련도가 높습니다**(음수).

**이 프로젝트에서** — `bm25(page_fts, 0.0, 5.0, 1.0)`. 제목을 본문보다 5배로 봅니다.

### snippet · 발췌 / highlight · 강조

검색 결과에서 매칭된 부분 주변만 잘라 보여주는 것과, 그 안에서 일치 부분을 표시하는
것.

**이 프로젝트에서** — FTS5 `snippet()`이 제어문자로 표시를 남기면, HTML 이스케이프를
먼저 한 뒤 그 표시를 `<mark>`로 바꿉니다. 순서를 뒤집으면 본문의 태그가 살아납니다.

### full scan · 전체 스캔

인덱스를 타지 못하고 모든 행을 훑는 것. `LIKE '%…%'`가 대표적입니다.

**이 프로젝트에서** — 짧은 쿼리의 폴백 경로. 규모가 커지면 가장 먼저 손봐야 할
지점입니다.

---

## 마크업과 렌더링

### CommonMark

마크다운의 모호함을 없앤 표준 명세. 표와 취소선은 명세 밖이라 확장으로 켭니다.

**이 프로젝트에서** — `markdown-it-py`를 `commonmark` 프리셋으로 쓰고 표·취소선만
추가로 활성화합니다.

### AST · 추상 구문 트리 / token stream · 토큰 스트림

파서가 원문을 해석해 만든 중간 구조. 렌더러는 원문이 아니라 이걸 보고 HTML을 만듭니다.

**이 프로젝트에서** — 먼저 파싱해 토큰에서 링크 대상을 모으고, 그 목록으로 존재
여부를 한 번에 조회한 뒤 렌더링합니다. 링크마다 쿼리를 날리지 않기 위한 순서입니다.

### inline rule · 인라인 규칙

markdown-it에서 문단 안쪽 문법(링크, 강조 등)을 처리하는 파서 규칙. 새 문법은 여기에
끼워 넣습니다.

**이 프로젝트에서** — `[[위키링크]]` 규칙을 기본 `link` 규칙 앞에 등록합니다.

### render cache · 렌더 캐시

마크다운 → HTML 변환 결과를 저장해두는 것. 변환은 읽기마다 반복하기엔 비쌉니다.

**이 프로젝트에서** — `render_cache` 테이블, `rev_id`가 키. 본문은 불변이지만 링크의
빨강/파랑은 따로 바뀌므로, 링크가 어떻게 해결됐는지의 해시(`link_state`)를 함께
저장해 그 부분만 무효화합니다.

### escaping · 이스케이프

`<`, `&` 같은 문자를 `&lt;`, `&amp;`로 바꿔 마크업이 아닌 글자로 취급되게 하는 것.

**이 프로젝트에서** — 원문의 raw HTML 자체를 끄고(`html: False`), 링크 라벨과 검색
발췌도 이스케이프합니다.

### XSS · Cross-Site Scripting

사용자가 넣은 내용이 스크립트로 실행되는 취약점. 위키는 본문을 그대로 HTML로 만드는
구조라 특히 직결됩니다.

**이 프로젝트에서** — raw HTML 차단과 이스케이프가 그 방어선입니다.

### percent-encoding · 퍼센트 인코딩

URL에 쓸 수 없는 문자를 `%ED%99%88`처럼 바이트 단위로 바꾸는 방식. **URL 인코딩**과
같은 말.

**이 프로젝트에서** — 한글 슬러그를 `href`에 넣을 때 적용합니다.

---

## 웹과 API

### ASGI

비동기 파이썬 웹 서버와 앱 사이의 표준 규약. WSGI의 비동기판.

**이 프로젝트에서** — `main.py`의 `app` 객체가 ASGI 앱이고, uvicorn이 그걸 실행합니다.

### endpoint · 엔드포인트 / route · 라우트 / router · 라우터

호출 가능한 하나의 주소, 그 주소와 처리 함수의 연결, 그리고 그런 연결을 묶은 단위.

**이 프로젝트에서** — `omnilog/api/` 아래 `pages`, `redirects`, `search` 라우터.

### CRUD

Create / Read / Update / Delete. 자원에 대한 기본 조작 네 가지.

### idempotent · 멱등

같은 요청을 여러 번 보내도 결과가 한 번 보낸 것과 같은 성질. `PUT`과 `DELETE`는
멱등이어야 하고 `POST`는 아닙니다.

**이 프로젝트에서** — 같은 별칭을 두 번 추가해도 하나만 생깁니다.

### path parameter / query parameter

경로에 박히는 값(`/pages/{slug}`)과 `?` 뒤에 붙는 값(`?limit=50`).

### status code · 상태 코드

이 프로젝트에서 쓰는 것들:

| 코드 | 뜻 |
|---|---|
| `201` | 생성됨 |
| `204` | 성공했고 본문 없음 |
| `400` | 잘못된 요청 |
| `404` | 없음 |
| `409` | 충돌 (슬러그 중복, 편집 충돌) |
| `413` | 본문 너무 큼 |
| `422` | 요청 형식 검증 실패 |

### OpenAPI / Swagger

HTTP API의 구조를 기계가 읽을 수 있게 기술한 규격과, 그걸로 만드는 대화형 문서.

**이 프로젝트에서** — FastAPI가 자동 생성. `/docs`와 `/openapi.json`.

### dependency injection · 의존성 주입

핸들러가 필요한 것을 직접 만들지 않고 밖에서 받도록 하는 방식.

**이 프로젝트에서** — `Depends(get_conn)`으로 요청마다 DB 연결을 받고, 끝나면
자동으로 닫습니다.

### lifespan · 수명 주기

앱이 뜰 때와 내려갈 때 한 번씩 실행되는 구간.

**이 프로젝트에서** — 여기서 스키마를 생성합니다.

### threadpool · 스레드풀

동기 함수를 이벤트 루프를 막지 않고 돌리기 위한 별도 스레드 묶음.

**이 프로젝트에서** — 핸들러를 `async def`가 아닌 `def`로 두면 FastAPI가 여기서
실행합니다. `sqlite3`는 동기 라이브러리라 이 방식이 맞습니다.

### pydantic / schema · 스키마 / validation · 검증

요청과 응답의 모양을 파이썬 타입으로 선언하고 자동으로 검사·변환하는 라이브러리와 그
선언, 검사 과정.

**이 프로젝트에서** — `omnilog/schemas.py`.

---

## 문자와 인코딩

### UTF-8

유니코드 문자를 바이트로 바꾸는 표준 방식. 한글 한 글자는 보통 3바이트라, **글자 수와
바이트 수가 다릅니다.**

**이 프로젝트에서** — 본문 크기 제한은 바이트 기준입니다.

### NFC / NFD · 유니코드 정규화

같은 글자를 표현하는 여러 바이트열을 하나로 통일하는 규칙. NFC는 `한`을 한 덩어리로,
NFD는 `ㅎ+ㅏ+ㄴ`으로 분해해 담습니다. **눈에는 똑같지만 문자열 비교에서는 다릅니다.**

**이 프로젝트에서** — 슬러그를 만들 때 NFC로 정규화합니다. macOS에서 넘어온 NFD
제목도 같은 슬러그로 떨어지게 하기 위해서입니다.

### codepage · 코드 페이지 / cp949

윈도우 콘솔이 쓰는 레거시 문자 인코딩. UTF-8 출력이 깨져 보이는 원인.

**이 프로젝트에서** — 데이터 문제가 아니라 표시 문제입니다. `PYTHONIOENCODING=utf-8`로
피할 수 있습니다.

---

## 도구

### uv

파이썬 패키지·가상환경 관리 도구. `uv sync`가 `pyproject.toml`과 `uv.lock`을 보고
환경을 맞춥니다.

### dependency group · 의존성 그룹

개발에만 필요한 패키지를 본 의존성과 분리해두는 `pyproject.toml`의 구획.

**이 프로젝트에서** — `dev` 그룹에 pytest와 httpx.

### lockfile · 잠금 파일

해석된 의존성 버전을 못 박아 어디서든 같은 환경이 재현되게 하는 파일.

**이 프로젝트에서** — `uv.lock`.

### fixture · 픽스처

테스트가 필요로 하는 준비물을 만들어 넘겨주는 pytest 장치.

**이 프로젝트에서** — `tests/conftest.py`. 테스트마다 임시 디렉터리에 새 DB를 만들어
서로 간섭하지 않게 합니다.

### smoke test · 스모크 테스트

"일단 켜지고 기본 동작은 하는가"를 확인하는 얕고 넓은 점검.
